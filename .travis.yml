language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-wordpress' ]; then cd .. && eval "mv $currentfolder generator-wordpress" && cd generator-wordpress; fi

